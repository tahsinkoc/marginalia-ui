{
  "name": "marginalia-workspace",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @marginalia/ui && npm run build --workspace @marginalia/docs",
    "dev": "npm run build --workspace @marginalia/ui && concurrently -n ui,docs -c auto \"npm run dev --workspace @marginalia/ui\" \"npm run dev --workspace @marginalia/docs\"",
    "test": "npm run test --workspace @marginalia/ui"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}


